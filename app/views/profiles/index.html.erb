<%= will_paginate @users %>

<h1>All users</h1>
<ul class="users">
  <% @users.each do |user| %>
      <li>
        <%= gravatar_for user, :size => 30 %>
        <%= link_to user.name, profile_path(user) %>
      </li>
  <% end %>
</ul>

<%= will_paginate @users %>
